{
  "name": "xprotect-camera-renamer",
  "version": "0.1.0",
  "private": true,
  "description": "Local Node tool to preview and apply XProtect camera naming changes through the API Gateway.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
